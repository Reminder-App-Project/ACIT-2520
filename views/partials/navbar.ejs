<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/reminders">Reminder List</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
    <% if(locals.condition_list) { %>
      <a class="nav-item nav-link <%= condition_list %>" href="/reminders">Reminder List</a>
    <% } else { %>
    <% if (locals.display) { %>
      <a class="nav-item nav-link" style="display: <%= display %>" href="/reminders">Reminder List</a>
    <% } else {%>
      <a class="nav-item nav-link" href="/reminders">Reminder List</a>
    <% }%>
    <% } %>

    <% if(locals.condition_new) { %>
      <a class="nav-item nav-link <%= condition_new %>" href="/reminder/new">Create</a>
    <% } else { %>
    <% if (locals.display) { %>
      <a class="nav-item nav-link" style="display: <%= display %>" href="/reminder/new">Create</a>
      <form method="POST" action="/logout">
        <button class="nav-item nav-link" style="display: <%= display %>">Logout</button>
      </form>
    <% } else {%>
      <a class="nav-item nav-link" href="/reminder/new">Create</a>
      <form method="POST" action="/logout">
        <button class="btn btn-link" style="">Logout</button>
      <form method="POST" action="/logout"></form>
    <% }%>
    <% } %>
    </div>
  </div>
</nav>